name: Prisma Compute Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        run: pnpm prisma generate

      - name: Validate Prisma token
        env:
          PRISMA_SERVICE_TOKEN: ${{ secrets.PRISMA_SERVICE_TOKEN }}
        run: |
          if [ -z "$PRISMA_SERVICE_TOKEN" ]; then
            echo "Missing GitHub secret: PRISMA_SERVICE_TOKEN"
            exit 1
          fi
          bunx @prisma/cli@preview auth whoami

      - name: Deploy app to Prisma Compute
        env:
          PRISMA_SERVICE_TOKEN: ${{ secrets.PRISMA_SERVICE_TOKEN }}
          PRISMA_PROJECT: LogiSync
          PRISMA_APP: LogiSync
        run: bunx @prisma/cli@preview app deploy --project "$PRISMA_PROJECT" --app "$PRISMA_APP" --branch main --build-type nextjs --no-interactive --yes --trace
